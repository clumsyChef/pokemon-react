const Header = () => {
    return (
        <nav className="navbar">
            <a href="#">Home</a>
            <a href="#">Pokemons</a>
            <a href="#">Search</a>
        </nav>
    )
}

export default Header;
